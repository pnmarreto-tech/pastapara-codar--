#include <stdio.h>

    int main (){
        
        int distancia, tempo;
        scanf("%i", &distancia);
        tempo = distancia * 2;
        printf("%.0i minutos\n", tempo);

        return 0;
    }